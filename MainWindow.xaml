<Window x:Class="GaokaoMathTrainer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="高考数学刷题" Height="720" Width="1000">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="高考数学刷题" FontSize="28" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock x:Name="StatusText" Grid.Row="1" Text="加载中..." FontSize="14" Foreground="Gray" Margin="0,0,0,24"/>

        <UniformGrid Grid.Row="2" Columns="3" Rows="2">
            <Button Content="顺序刷题" Click="OnSequentialClick" Margin="8" Height="120" FontSize="18"/>
            <Button Content="筛选刷题" Click="OnFilteredClick" Margin="8" Height="120" FontSize="18"/>
            <Button Content="分类刷题" Click="OnCategoryClick" Margin="8" Height="120" FontSize="18"/>
            <Button Content="错题练习" Click="OnMistakeClick" Margin="8" Height="120" FontSize="18"/>
            <Button Content="数据分析" Click="OnAnalysisClick" Margin="8" Height="120" FontSize="18"/>
            <Button Content="关于项目" Click="OnAboutClick" Margin="8" Height="120" FontSize="18"/>
        </UniformGrid>
    </Grid>
</Window>